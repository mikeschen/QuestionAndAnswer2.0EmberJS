import { questionTotal } from 'question-answer/helpers/question-total';
import { module, test } from 'qunit';

module('Unit | Helper | question total');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = questionTotal([42]);
  assert.ok(result);
});
